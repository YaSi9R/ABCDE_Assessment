version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - GIN_MODE=release
    volumes:
      - ./shopcart.db:/root/shopcart.db
    networks:
      - shopcart-network

networks:
  shopcart-network:
    driver: bridge
